import {render,fireEvent} from '@testing-library/svelte'; import {expect,it,vi} from 'vitest'; import ContentList from './ContentList.svelte';
const mk=(id:string)=>({id,kind:'text',retention:'temporary',title:id,preview:null,createdAt:'',updatedAt:'',cleanupAt:null,capabilities:{copyText:true,copyImage:false,copyFile:false,copyPath:false,openUrl:false,revealSensitive:false,edit:true,save:true,unsave:false,delete:true,reorder:true}} as any);
it('moves keyboard selection and hides drag affordance when disabled',async()=>{const select=vi.fn();const v=render(ContentList,{props:{items:[mk('a'),mk('b')],selectedId:'a',reorderable:false,onSelect:select,onReorder:vi.fn(),onToggleSaved:vi.fn(),onCopy:vi.fn(),onDelete:vi.fn()}});await fireEvent.keyDown(v.getByRole('listbox'),{key:'ArrowDown'});expect(select).toHaveBeenCalledWith('b');expect(v.queryByLabelText('拖动排序')).toBeNull();});
